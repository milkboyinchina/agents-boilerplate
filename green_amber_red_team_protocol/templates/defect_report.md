# Defect Report — packet `PACKET_NAME` (author: red)

> Fill this file. It is the ONLY verdict format `review-redteam` parses.
> Append, author-tagged. Never rewrite prior sections.

- **Verdict**: PASS | FAIL
- **Brief version**: BRIEF_VERSION (must match the inbound brief stamp)
- **Acks done**: packet_green_YYYYMMDD_HHMM (inbound packet consumed; archive-safe)
- **Still open**: (packet names explicitly left open, with reason — or "none")

## Probes run

| # | Probe | Expected | Observed | Pass? |
|:--|:---|:---|:---|:---:|
| 1 | | | | |

## Defect matrix (FAIL only)

| Bug ID | Severity | Probe | Expected | Observed | Artifact ref |
|:---|:---|:---|:---|:---|:---|

## Red-team-B appendix (sequential dual-red only)

Second team appends its own Probes + Matrix sections below, tagged `(red-b)`.
Either FAIL = FAIL (fail-closed; Green arbitrates divergence).
